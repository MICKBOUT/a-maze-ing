recherche: Mboutte
    Algo trouver un algo qui gen le maze (1)
    Algo qui find une soluc (3) diff ou 2 si flm




take congfig file:
    WIDTH
    HEIGHT
    ENTRY=(x, y)
    EXIT=(x, y)
    OUTPUT_FILE
    PERFECT

    ALGO -> choice path
    optionelle: output view isometrique (optionelle terminale)
    size_case=en pixel (de base en choisir une aproprié)


class MazeGenerator:
    methode:
        new maze:
            arg (dotenv) -> seed(int), perfect(bool), start(x, y), end(x, y)
        find path:
            arg (dotenv): algo
    remonter d'erreur dans les cas ou il y a un probleme a l'execusion (normalement ca ne devrais pas etre le cas)

Mlx:
    input:
        Le name d'un file
        contenue du ficher:
            (hexa valeur de chaque case) \n a chaque line
            line \n
            position start (x, y)\n
            position end (x, y)\n
            string(N | S | E | W) (path)\n

    des boutons:
        - show / Hide path
        - New color
        - New maze
        - Exit
        - (Slider qui tourne autour du maze (en mode maquette))
    une vue isiometrique (touche bind or slider)
    un algo qui choice les colors (compelentaire)
    un fenetre re-sizable (si possible)

un beau makefile (toml):
    une bonne gestion des dependances

un parsing qui repere les grille impossible:
    plus petit que le logo 42
    verifier si un chemin existe (test á l'execusion)
    verifer que les points entrer en sortie en se superpose pas
    (verifer que les point entrer et sortie en sont pas sur le logo 42) ou (remplacer le logo par le tour)

Une bonne gestion des try
Un code optimisé aux petits oignons


edge case a tester:
    - grille petite 0, 1, 2, 3, 4 px de longeur / largeur
    - grille plus petite que le logo 42
    - grille de la taille du logo 42
    - start et end au meme point sur la grille
    - grille tres grande (temps d'execusion)
    - entrer sur le logo 42
    - exit sur le logo 42

    verifier que tout les points dans toutes les grilles sont accesible
    verfier que un mur d'une  case correspond a un mur sur une case adjacante

ps: corriger les fautes de frappe dans ce ficher